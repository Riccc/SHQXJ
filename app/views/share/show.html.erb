<!DOCTYPE html>
<html lang="zh">
<head>
  <meta charset="UTF-8">
  <title>我的天气预报</title>
  <style>
    body {
      margin: 0;
      font-family: Arial, sans-serif;
      background: #f0f2f5;
    }

    /* 修改主容器样式 */
    #main-container {
      display: flex;
      justify-content: space-between;
      padding: 20px;
      height: calc(100vh - 40px); /* 限制为视口高度减去padding */
      box-sizing: border-box; /* 包含padding在高度计算内 */
    }

    /* 恢复背景图圆角并调整容器 */
    .container-16-9, .container-4-3 {
      position: relative;
      background-size: cover; /* 改回cover保持填充 */
      background-position: center;
      border-radius: 15px; /* 确保圆角存在 */
      overflow: hidden;
      width: 100%;
      height: 100%;
    }

    /* 移除之前的高度比例控制 */
    .container-16-9::before,
    .container-4-3::before {
      content: none; /* 移除伪元素的高度控制 */
    }

    .container-4-3::before {
      padding-top: 75%; /* 4:3 */
    }

    /* 修改卡片容器样式 */
    #card-container {
      flex: 1;
      margin-right: 20px;
      height: 100%; /* 继承父容器高度 */
    }

    #side-content {
      width: 25%;
      background: #e0e0e0;
      padding: 20px;
      border-radius: 10px;
    }

    /* 头像 */
    #avatar {
      position: absolute;
      top: 25%;  /* 下移头像位置 */
      left: 50%;
      transform: translateX(-50%);
      border-radius: 50%;
      border: 4px solid white;
      width: 120px;
      height: 120px;
      object-fit: cover;
      z-index: 2;
    }

    /* 名字 */
    #name-label {
      position: absolute;
      top: calc(25% + 120px + 15px);  /* 基于头像位置计算 */
      left: 50%;
      transform: translateX(-50%);
      font-size: 1.2rem;
      font-weight: bold;
      background: rgba(255, 255, 255, 0.7);
      padding: 4px 10px;
      border-radius: 8px;
      z-index: 2;
    }

    /* 顶部文字区域 */
    #text-container {
      position: absolute;
      top: 5%;  /* 上移文字区域 */
      left: 50%;
      transform: translateX(-50%);
      text-align: center;
      width: 90%;
      z-index: 2;
      margin-bottom: 20px; /* 增加下边距 */
    }

    #line1, #line2 {
      margin: 0;
      font-weight: bold;
      color: #333;
    }

    #line1 {
      font-size: 2rem;
    }

    #line2 {
      font-size: 1.4rem;
      white-space: nowrap;
    }

    /* 新增的CSS样式 */
#info-container {
  position: absolute;
  bottom: 10px;
  left: 50%;
  transform: translateX(-50%);
  width: 90%;
  height: auto;
  display: flex;
  justify-content: space-between;
  align-items: stretch;
  background: rgba(255, 255, 255, 0.6);
  padding: 10px;
  border-radius: 12px;
  font-size: 0.85rem;
  box-sizing: border-box;
}

.info-column {
  flex: 1;
  padding: 0 8px;
  display: flex;
  flex-direction: column;
  justify-content: center;
  align-items: center; /* 新增：交叉轴居中对齐 */
}

/* 移除原来的左右对齐设置 */
.info-column:first-child,
.info-column:last-child {
  text-align: center; /* 统一设置为居中对齐 */
}
.image-column {
  flex: 0.8;
  display: flex;
  justify-content: center;
  align-items: center;
}

#info-container h3 {
  margin: 0 0 5px;
  font-size: 0.95rem;
}

/* 调整段落样式确保居中效果 */
#info-container p {
  margin: 2px 0;
  white-space: normal;
  width: 100%; /* 确保文本块宽度满容器 */
  text-align: center; /* 再次确认居中对齐 */
}

    /* 调整分割线 */
    #info-container hr {
      margin: 6px 0;
    }

    /* 天气信息段落特殊处理 */
    #info-container p:nth-child(3),
    #info-container p:nth-child(5) {
      font-weight: bold;
    }
  </style>
</head>
<body>
  <div id="main-container">
    <div id="card-container">
      <div id="card" class="container-16-9">
        <img id="avatar" />
        <div id="name-label"><%= @name %></div>

        <div id="text-container">
          <p id="line1">我是小小预报员</p>
          <p id="line2">今天我在上海气象博物馆预报天气</p>
        </div>

<!-- 修改底部信息区域的HTML结构 -->
<div id="info-container">
  <div class="info-column">
    <h3>我的预报</h3>
    <p><%= @chosen_date %></p>
    <p><%= @low_temp%>~<%= @high_temp %></p>
    <p><%= @chosen_text %></p>
  </div>
  
  <div class="image-column">
    <!-- 图片预留位置 -->
    <img id="weather-image" 
     src="<%= @chosen_img%>" 
     style="max-height: 100%; max-width: 100%; object-fit: contain;"
     alt="天气图片">
  </div>
  
<div class="info-column">
  <h3>生日天气</h3>
  <p><%= @birthday %></p>
  <% if @birthday_weather_data.present? %>
    <p><%= @birthday_weather_data[:avg_temp] %>℃ / <%= @birthday_weather_data[:max_temp] %>℃</p>
    <p><%= @birthday_weather_data[:weather_summary] %></p>
    <p>降水 <%= @birthday_weather_data[:precipitation] %>mm</p>
  <% else %>
    <p>超出数据范围啦，但是一定是个好天气。</p>
  <% end %>
</div>
</div>
      </div>
    </div>

<div id="side-content">
  <h3>分享当前预报</h3>
  <div id="qrcode"></div>
  <%= @qr_svg %>
  <p>扫描二维码分享你的天气预报</p>
</div>
  </div>
  <script>
    document.addEventListener("DOMContentLoaded", function () {
      const uploadedBackgroundImage = localStorage.getItem("uploadedBackgroundImage");
      const uploadedImage = localStorage.getItem("uploadedImage");

      const card = document.getElementById("card");
      const avatar = document.getElementById("avatar");

      if (uploadedBackgroundImage) {
        const img = new Image();
        img.onload = function () {
          const ratio = this.width / this.height;

          if (ratio >= 1.77) {
            card.classList.remove("container-4-3");
            card.classList.add("container-16-9");
          } else {
            card.classList.remove("container-16-9");
            card.classList.add("container-4-3");
          }

          card.style.backgroundImage = `url(${uploadedBackgroundImage})`;
        };
        img.src = uploadedBackgroundImage;
      }

      if (uploadedImage && avatar) {
        avatar.src = uploadedImage;
      }
    });
  </script>
</body>
</html>